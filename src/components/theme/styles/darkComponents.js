import c from './colors'

const components = {
  button: {
    background: c.lightRed,
    backgroundHover: c.red,
    backgroundPressed: c.darkRed,
    border: c.white,
    label: c.white,
  },
}

export default components
